{
  "name": "firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Security rule tests for firestore.rules, run against the Firestore emulator.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project rules-test \"node rules.test.mjs\""
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  },
  "devDependencies": {
    "firebase-tools": "^14.0.0"
  }
}
