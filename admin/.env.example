# Only needed to override which Firebase project is targeted.
# Firebase config values are not secrets — they are hardcoded in src/lib/firebase.ts.
# The prod build sets VITE_ENV=prod in CI; dev is the default.
VITE_ENV=prod
