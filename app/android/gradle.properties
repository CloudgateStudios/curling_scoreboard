org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Matches the Flutter template for AGP 9: keep the legacy DSL and external Kotlin
# plugin until the Flutter Gradle plugin and plugins support the new defaults.
android.newDsl=false
android.builtInKotlin=false
